[project]
name = "eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/eval"]

[tool.uv.sources]
core = { workspace = true }
